## 팩토리 메소드 패턴을 적용했을 때의 장점은? 단점은?
### ✅ 장점
1. **객체 생성 로직의 캡슐화**
    - 클라이언트는 객체 생성 방식을 알 필요 없이 사용만 하면 된다.
2. **확장성 향상 (OCP 준수)**
    - 새로운 객체 타입 추가 시 기존 코드를 수정하지 않고 새로운 Creator/Factory만 추가하면 된다.
3. **결합도 감소**
    - 구체 클래스 대신 **추상 타입**에 의존하도록 하여 유연한 구조를 가진다.

### ⚠️ 단점
1. **클래스 수 증가**
    - 새로운 객체가 추가될 때마다 관련 Creator도 함께 추가되어 구조가 복잡해질 수 있다.
2. **불필요한 추상화 가능**
    - 단순한 객체 생성 상황에서는 오히려 코드 복잡도가 증가할 수 있다.
    - 
## 확장에 열려있고 변경에 닫혀있는 객체 지향 원칙"을 설명하세요.
### ✅ 정의 : OCP (Open-Closed Principle)
> 소프트웨어 요소는 **확장에는 열려 있고**, **변경에는 닫혀 있어야 한다.**
### ✅ 의미
- 요구사항이 변경되거나 기능이 추가될 때 **기존 코드를 수정하지 않고**
- **새로운 클래스를 추가하는 방식으로 확장**하는 구조를 지향한다.

### ✅ 예시
- 공통 인터페이스나 추상 클래스를 정의하고,
- 변경이 필요할 때 기존 클래스를 바꾸지 않고 **새로운 구현 클래스를 추가**하여 해결한다.

### 🔗 팩토리 메소드 패턴과의 연관성
- 새 제품(객체)을 추가할 때 기존 생성 로직을 변경하지 않고 **새로운 Factory/Creator만 추가**하면 되므로 OCP를 자연스럽게 만족한다.

## 자바 8에 추가된 Default 메소드 설명
### ✅ 정의
- 인터페이스에서 `default` 키워드를 사용하여 **메소드 구현을 제공할 수 있는 기능**.

### ✅ 도입 이유
- 인터페이스에 새로운 메소드가 추가될 때,
  기존 모든 구현체가 변경되어야 하는 문제를 해결하기 위함 (하위 호환성 유지).

### ✅ 특징
- 필요하면 구현 클래스에서 자유롭게 오버라이드 가능.
- 다중 상속 충돌 시 직접 선택해야 함.